# CLAP extension support matrix

> **TODO** — write this when M2 lands. Should list every CLAP extension
> we host, which milestone introduces it, and a link to the upstream
> header in `free-audio/clap`. Acts as the source of truth for "what
> the crate supports right now."
>
> Suggested columns: extension id (e.g. `clap.audio-ports`), milestone,
> upstream header URL, notes (host-side gotchas, wasm-specific quirks).
