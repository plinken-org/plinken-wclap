{
  "name": "@plinken/pulze",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pulze WCLAP drum machine (com.plinken)",
  "scripts": {
    "build:wasm": "cargo build -p com-plinken-pulze --target wasm32-unknown-unknown --release && mkdir -p dist && cp ../../../target/wasm32-unknown-unknown/release/com_plinken_pulze.wasm dist/pulze.wclap.wasm",
    "build": "pnpm run build:wasm && node ../../../scripts/bundle-wclap.mjs ."
  }
}
