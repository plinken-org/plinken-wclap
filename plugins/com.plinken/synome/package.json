{
  "name": "@plinken/synome",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Synome WCLAP synthesizer (com.plinken)",
  "scripts": {
    "build:wasm": "cargo build -p com-plinken-synome --target wasm32-unknown-unknown --release && mkdir -p dist && cp ../../../target/wasm32-unknown-unknown/release/com_plinken_synome.wasm dist/synome.wclap.wasm",
    "build": "pnpm run build:wasm && node ../../../scripts/bundle-wclap.mjs ."
  }
}
