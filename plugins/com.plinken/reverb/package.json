{
  "name": "@plinken/reverb",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Plinken Freeverb-style reverb (com.plinken)",
  "scripts": {
    "build:wasm": "cargo build -p com-plinken-reverb --target wasm32-unknown-unknown --release && mkdir -p dist && cp ../../../target/wasm32-unknown-unknown/release/com_plinken_reverb.wasm dist/reverb.wclap.wasm",
    "build": "pnpm run build:wasm && node ../../../scripts/bundle-wclap.mjs ."
  }
}
