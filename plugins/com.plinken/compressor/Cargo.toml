[package]
name = "com-plinken-compressor"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wclap-plugin = { path = "../../../crates/wclap-plugin", default-features = false }
